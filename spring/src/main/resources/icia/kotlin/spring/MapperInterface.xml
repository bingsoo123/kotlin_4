<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="icia.kotlin.spring.MapperInterface">  <!-- 인터페이스가 존재하는 곳의 주소 -->
	<select id="getData2" resultType="String">  <!-- id값은 메서드의 이름과 일치해야함  // resultType = 리턴타입 -->
		SELECT SYSDATE FROM DUAL
	</select>
</mapper>
